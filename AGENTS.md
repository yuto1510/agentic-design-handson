# AGENTS.md

このリポジトリは **architecture exercise（責務分担を考えるハンズオン）** です。
動くコードを増やすことではなく、「どこまでを LLM が判断し、どこからをコードが保証するか」
の境界を体験することが目的です。

エージェントは以下のルールを常に守ってください。

- 用意された CLI / function は **black box** として扱う。
  ドキュメント（`--help` や docstring の説明）に書かれた入出力だけを前提にする。
- `data/` を直接読まない。`cat` / `Read` / `grep` / `python` などいかなる手段でも中身を覗かない。
  データへのアクセスは必ず用意されたインターフェース経由で行う。
- tool / CLI の**内部実装を読んで答えを導かない**。
  実装を読むのは「参加者が指示した改修を行うとき」だけに限る。
- **他ブランチや解答例を見ない。**
  `git show <other-branch>:...` / `git diff` / `git log -p` などで他ブランチの内容を参照しない。
- **`WORKSHOP.md` と `README.md` を読まない。**
  これは参加者（人間）向けの進行資料で、観察ポイントと解答例の説明が入っている。
  読むと演習が成立しない。必要な指示はすべて参加者がプロンプトで渡す。
  読んでよいドキュメントはこの `AGENTS.md` と、tool / CLI の `--help` だけ。
- underlying data を直接読んで回答しない。
  「答えを先に知る」ことはこの演習では失敗であって、近道ではない。
- 追加の依存ライブラリを入れない（Python 標準ライブラリのみ）。

行き詰まったときは、勝手に禁止された手段へ回り込まず、
「用意されたインターフェースでは何が足りないか」を参加者に報告してください。

## 実行環境ごとの強制力

`data/` / `WORKSHOP.md` / `README.md` の読み取り禁止は、Claude Code では
`.claude/settings.json` の `permissions.deny` で**強制**されている。
Codex には該当する設定がなく（`.codex/config.toml` で固定できるのは書き込み範囲と
ネットワークだけ）、**この文書の指示だけ**が境界になる。
強制されていない環境でも、同じように守ること。
